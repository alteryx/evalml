[build-system]
requires = [
    "setuptools >= 47",
    "wheel"
]
build-backend = "setuptools.build_meta" 

[tool.coverage.run]
source = ["evalml"]

[tool.coverage.report]
exclude_lines =[
    "pragma: no cover",
]
